#include <stdio.h>
#include "4-sum.h"

/**
 * main - uses the sum function to sum 2 num
 *
 * Return: returns a 0 when correct.
 */
int main(void)
{
	int ans;

	ans = SUM(8100, 23);
	printf("%d\n", ans);
	return (0);
}
